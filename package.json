{
  "name": "memrie-cli",
  "version": "0.2.3",
  "description": "Memory product - context builder, embedding cache, tiered memory",
  "type": "module",
  "scripts": {
    "serve": "bun run src/server.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "bun test",
    "memrie": "bun run src/cli.ts",
    "cli": "bun run src/cli.ts"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "bin": {
    "memrie": "src/cli.ts"
  }
}
